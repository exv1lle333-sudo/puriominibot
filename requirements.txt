aiogram==3.15.0
aiosqlite==0.20.0
python-dotenv==1.0.1
httpx==0.27.2

# Веб-сервер мини-приложения Purio App (webapp/server.py).
# aiohttp уже тянется как зависимость aiogram, версия здесь зафиксирована
# на всякий случай, если вы ставите webapp отдельно от бота.
aiohttp==3.10.11

# Официальный SDK Remnawave. Версия жёстко привязана к версии панели —
# 3.2.3 сверена побайтово с backend-contract вашей панели (3.2.3).
# Если обновите панель на другую ветку — проверьте таблицу совместимости
# на https://pypi.org/project/remnactual/ и поправьте версию здесь.
remnactual==3.2.3
